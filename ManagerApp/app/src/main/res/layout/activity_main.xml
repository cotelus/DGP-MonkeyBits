<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="368dp"
        android:layout_height="495dp"
        android:orientation="vertical"
        tools:layout_editor_absoluteX="8dp"
        tools:layout_editor_absoluteY="8dp">

        <TextView
            android:id="@+id/textIDManager"
            android:layout_width="match_parent"
            android:layout_height="41dp"
            android:contentDescription="type the id of the new manager"
            android:text="@string/descrp_bt_NM" />

        <Button
            android:id="@+id/add"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="add new manager"
            android:text="Add" />

        <Button
            android:id="@+id/delete"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="delete manager"
            android:text="Delete" />

        <TextView
            android:id="@+id/list_admin"
            android:layout_width="match_parent"
            android:layout_height="359dp"
            android:text="list of the managers" />

    </LinearLayout>
</android.support.constraint.ConstraintLayout>